
export type TransformerConfig = {
    vocabSize: number;
    embeddingSize: number;
    numLayers: number;
    numHeads: number;
    hiddenSize: number;
    feedForwardSize: number;
    maxSequenceLength: number;
};
